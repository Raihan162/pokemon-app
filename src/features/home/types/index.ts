export interface Category {
  id: string;
  name: string;
  color: string;
  shadowColor: string;
  hoverShadowColor: string;
}

export interface NewsItem {
  id: string;
  title: string;
  date: string;
  imageUrl: string;
  gradient: string;
  pokemonName: string;
}